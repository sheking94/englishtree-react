import React from 'react';

import { Typography } from '@material-ui/core';

const StudentPanel = () => {
  return <Typography component="p">StudentPanel</Typography>;
};

export default StudentPanel;
